<?xml version="1.0"?>
<odoo>
		<record id="action_product_category_company" model="ir.actions.server">
        <field name="name">Account Properties</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="product.product_category_list_view"/>
        <field name="binding_model_id" ref="product.product_category_list_view"/>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.view_wizard()
        </field>
    </record>
</odoo>